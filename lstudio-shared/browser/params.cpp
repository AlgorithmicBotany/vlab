#include <fw.h>

#include "params.h"

/*
#include "..\resource.h"

static HICON Icons[VLB::Parameters::iIconCount];

const UINT IconNm[] =
{
	IDI_VLBOBJ,
	IDI_VLBEXT,
	IDI_ICON2
};

HICON VLB::Parameters::GetIcon(VLB::Parameters::IconId id)
{
	if (0 == Icons[id])
		Icons[id] = 
		(HICON)LoadImage
		(
		App::GetInstance(), 
		MAKEINTRESOURCE(IconNm[id]), IMAGE_ICON, 16, 16, LR_DEFAULTCOLOR
		);
	return Icons[id];
}
*/
